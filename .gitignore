# Credenciais e secrets — NUNCA versionar
.env
.env.*
*.pem
*.key
credentials
aws-credentials*
**/.aws/

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars

# Progresso (<curso>/.sessions/) é VERSIONADO de propósito: cada aluno commita o
# próprio progresso no seu fork/branch para continuar de qualquer máquina.
# A branch main do repo principal deve ficar sempre com progresso zerado.

# Configurações locais do Claude Code (por máquina, não versionar)
.claude/settings.local.json

# Python
__pycache__/
*.pyc

# Sistema / editores
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
